'use client'
import { useCart } from "../CartContext";

export default function CartCard() {
    const { state, dispatch } = useCart();
    return (
        <div>
            
        </div>
    );
};